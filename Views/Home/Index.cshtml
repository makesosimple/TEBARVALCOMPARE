@{ 
    var numberOfProjects = String.Format("{0:####}",ViewBag.numberOfProjects);
    var numberOfProjectsInAMonth = String.Format("{0:####}", ViewBag.numberOfProjectsInAMonth);
    var numberOfCompletedProjects = String.Format("{0:N}",ViewBag.numberOfCompletedProjects * 100);
    var myShortcuts = ViewBag.myShortcuts;
    var serviceAreaList = ViewBag.serviceAreaList;
    var projectCountByYear = ViewBag.projectCountByYear;
}
<div class="row">
    <div class="justify-content-between py-2 col-12 d-flex">
        <h1 class="h3 text-gray-800">34 Proje Portal</h1>
     </div>
</div>

<!-- Content Row -->
<div class="row">

    <!-- Earnings (Monthly) Card Example -->
    <div class="col-xl-3 col-md-6 mb-4">
        <a href="/Project/">
            <div class="card border-left-primary shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                Projeler
                            </div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@numberOfProjects</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-project-diagram fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </a>
    </div>

    <!-- Earnings (Monthly) Card Example -->
    <div class="col-xl-3 col-md-6 mb-4">
        <a href="/ProjectMap/">
        <div class="card border-left-success shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                            Harita
                        </div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">PROJE HARİTASI</div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-map fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
        </a>
    </div>

    <!-- Earnings (Monthly) Card Example -->
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-info shadow h-100 py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                            Tamamlananlar
                        </div>
                        <div class="row no-gutters align-items-center">
                            <div class="col-auto">
                                <div class="h5 mb-0 mr-3 font-weight-bold text-gray-800">@numberOfCompletedProjects%</div>
                            </div>
                            <div class="col">
                                <div class="progress progress-sm mr-2">
                                    <div class="progress-bar bg-info" role="progressbar"
                                         style="width: @numberOfCompletedProjects%" aria-valuenow="@numberOfCompletedProjects" aria-valuemin="0"
                                         aria-valuemax="100"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-clipboard-list fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Pending Requests Card Example -->
    <div class="col-xl-3 col-md-6 mb-4">
        <a href="/Project/Create">
            <div class="card border-left-warning shadow h-100 py-2">
                <div class="card-body d-flex align-items-center justify-content-between">
                    <p class="text-lg font-weight-bold m-0 text-warning text-uppercase">YENİ PROJE OLUŞTUR</p>
                    <i class="fas fa-plus fa-2x text-gray-300"></i>
                </div>
            </div>
        </a>
    </div>
</div>

<!-- Content Row -->

<div class="row">

    <!-- Area Chart -->
    <div class="col-xl-8 col-lg-7">
        <div class="card shadow mb-4">
            <!-- Card Header - Dropdown -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Tarihe Göre Projeler</h6>
                <!--<div class="dropdown no-arrow">
                    <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in"
                         aria-labelledby="dropdownMenuLink">
                        <div class="dropdown-header">Dropdown Header:</div>
                        <a class="dropdown-item" href="#">Action</a>
                        <a class="dropdown-item" href="#">Another action</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>
                    </div>
                </div>-->
            </div>
            <!-- Card Body -->
            <div class="card-body">
                <div class="chart-area">
                    <canvas id="myAreaChart"></canvas>
                </div>
            </div>
        </div>
    </div>

    <!-- Pie Chart -->
    <div class="col-xl-4 col-lg-5">
        <div class="card shadow mb-4">
            <!-- Card Header - Dropdown -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Proje Dağılımı</h6>
                <div class="dropdown no-arrow">
                    <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in"
                         aria-labelledby="dropdownMenuLink">
                        <div class="dropdown-header">Dropdown Header:</div>
                        <a class="dropdown-item" href="#">Action</a>
                        <a class="dropdown-item" href="#">Another action</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Something else here</a>
                    </div>
                </div>
            </div>
            <!-- Card Body -->
            <div class="card-body">
                <div class="chart-pie pt-4 pb-2">
                    <canvas id="myPieChart"></canvas>
                </div>
                <div class="mt-4 text-center small" id="chartLegend">
                    @foreach (var item in serviceAreaList)
                    {
                        <span class="mr-2">
                            <i class="fas fa-circle"></i> @item.ServiceAreaTitle
                        </span>
                    }

                    
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Content Row -->
<div class="row">

    <!-- Content Column -->
    <div class="col-lg-6 mb-4">

        <!-- Project Card Example -->
        <div class="card shadow mb-4 h-100">
            <div class="card-header py-3 bg-turqoise">
                <h6 class="m-0 font-weight-bold text-white">Kısayollarım</h6>
            </div>
            <div class="card-body d-flex flex-column justify-content-between">
                @foreach (var item in myShortcuts)
                {
                    <div class="shortcut-item mb-3">
                        <p class="text-primary text-md font-weight-bold mb-1">@item.ProjectTitle</p>
                        <a href="/Project/Edit/@item.ProjectID" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Projeye Git</a>
                    </div>
                }

               
            </div>
        </div>
    </div>

    <div class="col-lg-6 mb-4">

        <!-- Project Card Example -->
        <div class="card shadow mb-4 h-100">
            <div class="card-header py-3 bg-turqoise">
                <h6 class="m-0 font-weight-bold text-white">Güncellemeler</h6>
            </div>
            <div class="card-body d-flex flex-column justify-content-between">
                <div class="shortcut-item mb-3">
                    <p class="text-primary text-md font-weight-bold mb-1">Bilge Kobaş, 3400210201XX kodlu Arnavutköy Bilim Merkezi projesinde değişiklik yaptı.</p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Projeye Git</a>
                </div>
                <div class="shortcut-item mb-3">
                    <p class="text-primary text-md font-weight-bold mb-1">Cem Üstün, 3400210201XX kodlu proje için bir rapor üretti.</p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Rapora Git</a>
                </div>
                <div class="shortcut-item mb-3">
                    <p class="text-primary text-md font-weight-bold mb-1">Kentsel Tasarım Müdürlüğü, 3400210201XX kodlu Arnavutköy Bilim Merkezi projesi için görüş raporu talep etti. </p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Projeye Git</a>
                </div>
                <div class="shortcut-item">
                    <p class="text-primary text-md font-weight-bold mb-1">Selçuk Orhan, 3400210201XX kodlu Beyoğlu Hizmet Binası projesi için parsel bilgisi girdi.</p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Projeye Git</a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Content Row -->
<!--<div class="row">-->
<!-- Content Column -->
<!--<div class="col-12 mb-4">-->
<!-- Project Card Example -->
<!--<div class="card shadow mb-4 h-100">
            <div class="card-header py-3 bg-rose-pink">
                <h6 class="m-0 font-weight-bold text-white">Uyarılar</h6>
            </div>
            <div class="card-body d-flex flex-column justify-content-between">
                <div class="shortcut-item mb-3">
                    <p class="text-primary text-md font-weight-bold mb-1">Kentsel Tasarım Müdürlüğü, 3400210201XX kodlu Arnavutköy Bilim Merkezi projesi için görüş raporu talep etti. </p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i>Uyarıyı İncele</a>
                </div>
                <div class="shortcut-item mb-3">
                    <p class="text-primary text-md font-weight-bold mb-1">Üstyapı Daire Başkanlığı, 3400210201XX kodlu Beyoğlu Hizmet Binası projesinin sınırlarıyla çakışan bir Öneri Proje ekledi.</p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Uyarıyı İncele</a>
                </div>
                <div class="shortcut-item mb-3">
                    <p class="text-primary text-md font-weight-bold mb-1">Kentsel Tasarım Müdürlüğü, 3400210201XX kodlu Beyoğlu Hizmet Binası projesinin sınırlarıyla çakışan 3400210201XX kodlu X projesininin durumunu yapım olarak güncelledi.</p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Uyarıyı İncele</a>
                </div>
                <div class="shortcut-item">
                    <p class="text-primary text-md font-weight-bold mb-1">Kentsel Tasarım Müdürlüğü, 3400210201XX kodlu Beyoğlu Hizmet Binası projesi için Yönetim yetkisi istedi.</p>
                    <a href="#" class="text-gray-900"><i class="fas fa-arrow-right mr-1"></i> Uyarıyı İncele</a>
                </div>
            </div>
        </div>
    </div>
</div>-->

@section Scripts {
    <!-- Chart Demos -->
    <script src="~/lib/chart.js/Chart.min.js"></script>
    <script type="text/javascript">

        let colors = [
            '#3366CC',
            '#DC3912',
            '#FF9900',
            '#109618',
            '#990099',
            '#3B3EAC',
            '#0099C6',
            '#DD4477',
            '#66AA00',
            '#B82E2E',
            '#316395',
            '#994499',
            '#22AA99',
            '#AAAA11',
            '#6633CC',
            '#E67300',
            '#8B0707',
            '#329262',
            '#5574A6',
            '#3B3EAC',
        ];

        $('#chartLegend .fa-circle').each(function (i,elem) {
            elem.style = 'color:' + colors[i] + ';';
        });

        Chart.defaults.global.defaultFontFamily = 'Nunito', '-apple-system,system-ui,BlinkMacSystemFont,"Segoe UI",Roboto,"Helvetica Neue",Arial,sans-serif';
        Chart.defaults.global.defaultFontColor = '#858796';


        // Pie Chart Example
        var ctx = document.getElementById("myPieChart");
        var myPieChart = new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: [
                    @foreach (var item in serviceAreaList)
                    {
                     <text>
                    "@Html.Raw(item.ServiceAreaTitle)",
                    </text>
                    }
                ],
                datasets: [{
                    data: [
                        @foreach (var item in serviceAreaList)
                        {
                         <text>
                            @item.NumberOfProjects,
                        </text>
                        }
                    ],
                    backgroundColor: colors,
                    hoverBackgroundColor: colors,
                    hoverBorderColor: "rgba(234, 236, 244, 1)",
                }],
            },
            options: {
                maintainAspectRatio: false,
                tooltips: {
                    backgroundColor: "rgb(255,255,255)",
                    bodyFontColor: "#858796",
                    borderColor: '#dddfeb',
                    borderWidth: 1,
                    xPadding: 15,
                    yPadding: 15,
                    displayColors: false,
                    caretPadding: 10,
                },
                legend: {
                    display: false
                },
                cutoutPercentage: 80,
            },
        });
        //

        Chart.defaults.global.defaultFontFamily = 'Nunito', '-apple-system,system-ui,BlinkMacSystemFont,"Segoe UI",Roboto,"Helvetica Neue",Arial,sans-serif';
        Chart.defaults.global.defaultFontColor = '#0a1a33';

        function number_format(number, decimals, dec_point, thousands_sep) {
            // *     example: number_format(1234.56, 2, ',', ' ');
            // *     return: '1 234,56'
            number = (number + '').replace(',', '').replace(' ', '');
            var n = !isFinite(+number) ? 0 : +number,
                prec = !isFinite(+decimals) ? 0 : Math.abs(decimals),
                sep = (typeof thousands_sep === 'undefined') ? ',' : thousands_sep,
                dec = (typeof dec_point === 'undefined') ? '.' : dec_point,
                s = '',
                toFixedFix = function (n, prec) {
                    var k = Math.pow(10, prec);
                    return '' + Math.round(n * k) / k;
                };
            // Fix for IE parseFloat(0.55).toFixed(0) = 0;
            s = (prec ? toFixedFix(n, prec) : '' + Math.round(n)).split('.');
            if (s[0].length > 3) {
                s[0] = s[0].replace(/\B(?=(?:\d{3})+(?!\d))/g, sep);
            }
            if ((s[1] || '').length < prec) {
                s[1] = s[1] || '';
                s[1] += new Array(prec - s[1].length + 1).join('0');
            }
            return s.join(dec);
        }

        // Area Chart Example
        var ctx = document.getElementById("myAreaChart");
        var myLineChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: [
                    @foreach (var item in projectCountByYear)
                    {
                     <text>
                    "@item.ProjectYear",
                    </text>
                    }
                ],
                datasets: [{
                    label: "Projeler",
                    lineTension: 0.3,
                    backgroundColor: "rgba(78, 115, 223, 0.05)",
                    borderColor: "#0a1a33",
                    pointRadius: 3,
                    pointBackgroundColor: "#0a1a33",
                    pointBorderColor: "#0a1a33",
                    pointHoverRadius: 3,
                    pointHoverBackgroundColor: "#0a1a33",
                    pointHoverBorderColor: "#0a1a33",
                    pointHitRadius: 10,
                    pointBorderWidth: 2,
                    data: [
                        @foreach (var item in projectCountByYear)
                        {
                         <text>
                        "@item.NumberOfProjects",
                        </text>
                        }
                        ],
                }],
            },
            options: {
                maintainAspectRatio: false,
                layout: {
                    padding: {
                        left: 10,
                        right: 25,
                        top: 25,
                        bottom: 0
                    }
                },
                scales: {
                    xAxes: [{
                        time: {
                            unit: 'date'
                        },
                        gridLines: {
                            display: false,
                            drawBorder: false
                        },
                        ticks: {
                            maxTicksLimit: 7
                        }
                    }],
                    yAxes: [{
                        ticks: {
                            maxTicksLimit: 5,
                            padding: 10,
                            // Include a dollar sign in the ticks
                            callback: function (value, index, values) {
                                return number_format(value);
                            }
                        },
                        gridLines: {
                            color: "rgb(234, 236, 244)",
                            zeroLineColor: "rgb(234, 236, 244)",
                            drawBorder: false,
                            borderDash: [2],
                            zeroLineBorderDash: [2]
                        }
                    }],
                },
                legend: {
                    display: false
                },
                tooltips: {
                    backgroundColor: "rgb(255,255,255)",
                    bodyFontColor: "#858796",
                    titleMarginBottom: 10,
                    titleFontColor: '#6e707e',
                    titleFontSize: 14,
                    borderColor: '#dddfeb',
                    borderWidth: 1,
                    xPadding: 15,
                    yPadding: 15,
                    displayColors: false,
                    intersect: false,
                    mode: 'index',
                    caretPadding: 10,
                    callbacks: {
                        label: function (tooltipItem, chart) {
                            var datasetLabel = chart.datasets[tooltipItem.datasetIndex].label || '';
                            return datasetLabel + ': ' + number_format(tooltipItem.yLabel);
                        }
                    }
                }
            }
        });
        //DashboardLineGraphModel
    </script>
    
}