@model IBBPortal.Models.Board

@{
    ViewData["Title"] = "Kurul Ekle";
}

<h1 class="h3 mb-4 text-gray-800">@ViewData["Title"]</h1>
<form asp-action="Create" method="post">
    <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex flex-column flex-md-row align-items-center justify-content-between">
            <ul class="nav nav-pills" id="formTabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <a class="nav-link active" id="general-tab" data-toggle="tab" href="#general" role="tab" aria-controls="general" aria-selected="false">Genel</a>
                </li>
            </ul>
        </div>
        <div class="card-body">
            <div class="tab-pane fade active show" id="general" role="tabpanel" aria-labelledby="general-tab">
                <div class="row">
                    <div class="col-12 col-xl-6">
                        <!-- Validation Part. Must be still for all views!! -->
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <!-- Board Title -->
                        <div class="form-group row">
                            <label asp-for="@Model.BoardTitle" class="col-12 col-xl-4 col-form-label">Kurul İsmi<span class="text-danger">*</span></label>
                            <div class="col-12 col-xl-8">
                                <input asp-for="@Model.BoardTitle" class="form-control" required>
                                <span asp-validation-for="@Model.BoardTitle" class="text-danger"></span>
                            </div>
                        </div>

                        <!-- Description -->
                        <div class="form-group row">
                            <label asp-for="@Model.BoardDescription" class="col-12 col-xl-4 col-form-label">Kurul Açıklaması</label>
                            <div class="col-12 col-xl-8">
                                <textarea class="form-control" asp-for="@Model.BoardDescription" rows="3"></textarea>
                                <span asp-validation-for="@Model.BoardDescription" class="text-danger"></span>
                            </div>
                        </div>

                        <!-- Default Values for the Database Date Part-->
                        <input type="hidden" asp-for="CreationDate" value="@ViewBag.CurrentDate" />

                        <!-- User ID for the Creation -->
                        <input type="hidden" asp-for="UserID" value="@ViewBag.UserID" />

                        <div class="buttons mt-3 my-md-0">
                            <button type="submit" value="Save" class="btn bg-success text-white"><i class="fas fa-save mr-2"></i> Kaydet</button>
                            <a asp-action="Index" class="btn btn-danger"><i class="fas fa-trash-alt mr-2"></i> Vazgeç</a>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
