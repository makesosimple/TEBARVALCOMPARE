@model IBBPortal.Models.Project

@{
    ViewData["Title"] = @Model.ProjectTitle;
    var cultureInfo = CultureInfo.CreateSpecificCulture("tr-TR");
    var ProjectCost = Model.ProjectCost.HasValue ? Model.ProjectCost.Value.ToString("N2", cultureInfo) : "";
}

<!-- Alert for Exceptions -->
@if (@TempData["SuccessTitle"] is not null)
{
    <div class="alert bg-gradient-success alert-dismissible fade show border-0" role="alert">
        <h4 class="alert-heading text-white">
            <i class="fas fa-check"></i>
            @TempData["SuccessTitle"]
            <button type="button" class="close text-white" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </h4>
        <p class="text-white">@TempData["SuccessMessage"]</p>
    </div>
}

<div class="row">
    <div class="justify-content-between py-2 col-12 d-flex">
        <h1 class="h3">@ViewData["Title"]</h1>
    </div>
</div>
<div class="card shadow mb-4">
    @await Html.PartialAsync("~/Views/Project/_ManageNavigation.cshtml",
    new ViewDataDictionary(ViewData)
    {
        { "Id", @Model.ProjectID }
    })
    <div class="card-body">
        <div class="tab-content my-3" id="formContent">
            <div class="tab-pane fade active show" id="tag" role="tabpanel" aria-labelledby="tag-tab">
                <form asp-action="Edit">
                    <div class="row">
                        <div class="col-12 col-xl-6">
                            <input type="hidden" asp-for="ProjectID" />
                            <!-- Validation Part. Must be still for all views!! -->
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                            <!-- Project Title -->
                            <div class="form-group row">
                                <label asp-for="ProjectTitle" class="col-12 col-lg-4 col-form-label">Proje Başlığı<span class="text-danger">*</span></label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectTitle" type="text" class="form-control" required>
                                    <span asp-validation-for="ProjectTitle" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Code -->
                            <div class="form-group row">
                                <label asp-for="ProjectCode" class="col-12 col-lg-4 col-form-label">Proje Kodu</label>
                                <div class="col-12 col-lg-8">
                                    <div class="text-primary">@Model.ProjectCode</div>
                                </div>
                            </div>

                            <!-- Project IBB Code -->
                            <div class="form-group row">
                                <label asp-for="ProjectIBBCode" class="col-12 col-lg-4 col-form-label">Proje İBB Kodu</label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectIBBCode" class="form-control">
                                    <span asp-validation-for="ProjectIBBCode" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project End Time -->
                            <div class="form-group row">
                                <label asp-for="ProjectEndTime" class="col-12 col-lg-4 col-form-label">Proje Bitiş Tarihi</label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectEndTime" type="text" class="form-control datepicker">
                                    <i class="fas fa-calendar-alt"></i>
                                    <span asp-validation-for="ProjectEndTime" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Year -->
                            <div class="form-group row">
                                <label asp-for="ProjectYear" class="col-12 col-lg-4 col-form-label">Proje Yılı</label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectYear" type="number" class="form-control">
                                    <span asp-validation-for="ProjectYear" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Cost -->
                            <div class="form-group row">
                                <label asp-for="ProjectCost" class="col-12 col-lg-4 col-form-label">Proje Bedeli</label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectCost" type="text" class="form-control" value="@ProjectCost" pattern="^\$\d{1,3}(,\d{3})*(\.\d+)?$" data-input-type="length" data-val="false">
                                    <i class="fas fa-coins"></i>
                                    <span asp-validation-for="ProjectCost" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Type ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectTypeID" class="col-12 col-xl-4 col-form-label">Proje Türü</label>
                                <div class="col-12 col-xl-8">
                                    <select asp-for="ProjectTypeID" class="form-control" id="selectProjectType" style="width: 100%;">
                                        @if (@Model.ProjectType is not null)
                                        {
                                            <option value="@Model.ProjectType.Id">@Model.ProjectType.Title</option>
                                        }

                                        else
                                        {
                                            <option value="">Proje Türü Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectTypeID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Is Done by IBB ? -->
                            <div class="form-group row">
                                <label asp-for="IsDoneByIBB" class="col-form-label col-12 col-xl-4">Proje İBB tarafından yapılmamaktadır.<span class="text-danger">*</span></label>
                                <div class="col-12 col-xl-8">
                                    <div class="text-center d-inline-block col-form-label">
                                        <div class="vc-toggle-container">
                                            <label class="vc-switch">
                                                <input type="checkbox" class="vc-switch-input" asp-for="IsDoneByIBB">
                                                <span data-on="Evet" data-off="Hayır" class="vc-switch-label"></span>
                                                <span class="vc-handle"></span>
                                                <span asp-validation-for="IsDoneByIBB" class="text-danger"></span>
                                            </label>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- External Organization ID -->
                            <div class="form-group row" data-dependent-on="#IsDoneByIBB" data-dependent-value="checked">
                                <label asp-for="ExternalOrganizationID" class="col-12 col-xl-4 col-form-label">Projeyi Yöneten Kurum</label>
                                <div class="col-12 col-xl-8">
                                    <select asp-for="ExternalOrganizationID" class="form-control" id="selectExternalOrganization" style="width: 100%;">
                                        @if (@Model.ExternalOrganization is not null)
                                        {
                                            <option value="@Model.ExternalOrganization.OrganizationID">@Model.ExternalOrganization.OrganizationTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Kurum Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ExternalOrganizationID" class="text-danger"></span>
                                </div>
                            </div>

                            <hr />

                            <!-- Requesting Management ID -->
                            <div class="form-group row">
                                <label asp-for="RequestingDepartmentID" class="col-12 col-lg-4 col-form-label">Talep Eden Müdürlük</label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="RequestingDepartmentID" class="form-control" id="selectRequestingDepartment" style="width: 100%;">
                                        @if (@Model.RequestingDepartment is not null)
                                        {
                                            <option value="@Model.RequestingDepartment.DepartmentID">@Model.RequestingDepartment.DepartmentTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Talep Eden Müdürlük Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="RequestingDepartmentID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Responsible Management ID -->
                            <div class="form-group row">
                                <label asp-for="ResponsibleDepartmentID" class="col-12 col-lg-4 col-form-label">Sorumlu Müdürlük</label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="ResponsibleDepartmentID" class="form-control" id="selectResponsibleDepartment" style="width: 100%;">
                                        @if (@Model.ResponsibleDepartment is not null)
                                        {
                                            <option value="@Model.ResponsibleDepartment.DepartmentID">@Model.ResponsibleDepartment.DepartmentTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Sorumlu Müdürlük Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ResponsibleDepartmentID" class="text-danger"></span>
                                </div>
                            </div>

                            <hr />

                            <!-- Requesting Authority ID -->
                            <div class="form-group row">
                                <label asp-for="RequestingAuthorityID" class="col-12 col-lg-4 col-form-label">Talep Eden Makam</label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="RequestingAuthorityID" class="form-control" id="selectRequestingAuthority" style="width: 100%;">
                                        @if (@Model.RequestingAuthority is not null)
                                        {
                                            <option value="@Model.RequestingAuthority.AuthorityID">@Model.RequestingAuthority.AuthorityTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Talep Eden Makam Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="RequestingAuthorityID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Requesting Authority Description -->
                            <div class="form-group row">
                                <label asp-for="RequestingAuthorityDescription" class="col-12 col-xl-4 col-form-label">Talep Açıklaması</label>
                                <div class="col-12 col-xl-8">
                                    <textarea asp-for="RequestingAuthorityDescription" class="form-control"></textarea>
                                    <span asp-validation-for="RequestingAuthorityDescription" class="text-danger"></span>
                                </div>
                            </div>

                            <hr />

                            <!-- Project Owner Person ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectOwnerPersonID" class="col-12 col-lg-4 col-form-label">Proje Sahibi<span class="text-danger">*</span></label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="ProjectOwnerPersonID" class="form-control" id="selectProjectOwner" style="width: 100%;" required>
                                        @if (@Model.ProjectOwnerPerson is not null)
                                        {
                                            <option value="@Model.ProjectOwnerPerson.PersonID">@Model.ProjectOwnerPerson.PersonName @Model.ProjectOwnerPerson.PersonSurname</option>
                                        }

                                        else
                                        {
                                            <option value="">Proje Sahibi Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectOwnerPersonID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Manager ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectManagerID" class="col-12 col-lg-4 col-form-label">Proje Müdürü</label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="ProjectManagerID" class="form-control" id="selectProjectManager" style="width: 100%;">
                                        @if (@Model.ProjectManager is not null)
                                        {
                                            <option value="@Model.ProjectManager.PersonID">@Model.ProjectManager.PersonName @Model.ProjectManager.PersonSurname</option>
                                        }

                                        else
                                        {
                                            <option value="">Proje Müdürü Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectManagerID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Service Area ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectServiceAreaID" class="col-12 col-lg-4 col-form-label">Proje Hizmet Alanı<span class="text-danger">*</span></label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="ProjectServiceAreaID" class="form-control" id="selectServiceArea" style="width: 100%;" required>
                                        @if (@Model.ProjectServiceArea is not null)
                                        {
                                            <option value="@Model.ProjectServiceArea.ServiceAreaID">@Model.ProjectServiceArea.ServiceAreaTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Hizmet Alanı Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectServiceAreaID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Additional Service Area ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectAdditionalServiceAreaID" class="col-12 col-xl-4 col-form-label">Ek Hizmet Alanı</label>
                                <div class="col-12 col-xl-8">
                                    <select asp-for="ProjectAdditionalServiceAreaID" class="form-control" id="selectAdditionalServiceArea" style="width: 100%;">
                                        @if (@Model.ProjectAdditionalServiceArea is not null)
                                        {
                                            <option value="@Model.ProjectAdditionalServiceArea.ServiceAreaID">@Model.ProjectAdditionalServiceArea.ServiceAreaTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Ek Hizmet Alanı Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectAdditionalServiceAreaID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Importance ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectImportanceID" class="col-12 col-lg-4 col-form-label">Proje Önemi</label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="ProjectImportanceID" class="form-control" id="selectProjectImportance" style="width: 100%;">
                                        @if (@Model.ProjectImportance is not null)
                                        {
                                            <option value="@Model.ProjectImportance.ProjectImportanceID">@Model.ProjectImportance.ProjectImportanceTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Proje Önemi Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectImportanceID" class="text-danger"></span>
                                </div>
                            </div>

                        </div>
                        <div class="col-12 col-xl-6">
                            <!-- Project Title (Production) -->
                            <div class="form-group row">
                                <label asp-for="ProjectProductionName" class="col-12 col-lg-4 col-form-label">Proje Başlığı (Yapım)</label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectProductionName" type="text" class="form-control">
                                    <span asp-validation-for="ProjectProductionName" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Status Description Date -->
                            <div class="form-group row">
                                <label asp-for="ProjectProductionEndTime" class="col-12 col-lg-4 col-form-label">Proje Bitiş Tarihi (Yapım)</label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectProductionEndTime" type="text" class="form-control datepicker">
                                    <i class="fas fa-calendar-alt"></i>
                                    <span asp-validation-for="ProjectProductionEndTime" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Production Responsible Department ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectProductionRespDepartmentID" class="col-12 col-lg-4 col-form-label">Sorumlu Müdürlük (Yapım)</label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="ProjectProductionRespDepartmentID" class="form-control" id="selectProductionResponsibleDepartment" style="width: 100%;">
                                        @if (@Model.ProjectProductionRespDepartment is not null)
                                        {
                                            <option value="@Model.ProjectProductionRespDepartment.DepartmentID">@Model.ProjectProductionRespDepartment.DepartmentTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Sorumlu Müdürlük Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectProductionRespDepartmentID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Status Description Date -->
                            <div class="form-group row">
                                <label asp-for="RespDepartmentTransferDate" class="col-12 col-xl-4 col-form-label">Sorumlu Müdürlüğe Gönderim Tarihi (Yapım)</label>
                                <div class="col-12 col-xl-8">
                                    <input asp-for="RespDepartmentTransferDate" type="text" class="form-control datepicker">
                                    <i class="fas fa-calendar-alt"></i>
                                    <span asp-validation-for="RespDepartmentTransferDate" class="text-danger"></span>
                                </div>
                            </div>

                            <hr />

                            <!-- Production Production Status ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectProductionStatusID" class="col-12 col-xl-4 col-form-label">Yapım İşinin Durumu</label>
                                <div class="col-12 col-xl-8">
                                    <select asp-for="ProjectProductionStatusID" class="form-control" id="selectProjectProductionStatus" style="width: 100%;">
                                        @if (@Model.ProjectProductionStatus is not null)
                                        {
                                            <option value="@Model.ProjectProductionStatus.ProjectStatusID">@Model.ProjectProductionStatus.ProjectStatusTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Durum Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectProductionStatusID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Production Status Description -->
                            <div class="form-group row">
                                <label asp-for="ProjectProductionStatusDescription" class="col-12 col-xl-4 col-form-label">Yapım İşinin Durum Açıklaması</label>
                                <div class="col-12 col-xl-8">
                                    <textarea asp-for="ProjectProductionStatusDescription" class="form-control"></textarea>
                                    <span asp-validation-for="ProjectProductionStatusDescription" class="text-danger"></span>
                                </div>
                            </div>

                            <hr />

                            <!-- Project Status ID -->
                            <div class="form-group row">
                                <label asp-for="ProjectStatusID" class="col-12 col-lg-4 col-form-label">Proje Durumu<span class="text-danger">*</span></label>
                                <div class="col-12 col-lg-8">
                                    <select asp-for="ProjectStatusID" class="form-control" id="selectProjectStatus" style="width: 100%;" required>
                                        @if (@Model.ProjectStatus is not null)
                                        {
                                            <option value="@Model.ProjectStatus.ProjectStatusID">@Model.ProjectStatus.ProjectStatusTitle</option>
                                        }

                                        else
                                        {
                                            <option value="">Proje Durumu Seçin</option>
                                        }
                                    </select>
                                    <span asp-validation-for="ProjectStatusID" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Status Description -->
                            <div class="form-group row">
                                <label asp-for="ProjectStatusDescription" class="col-12 col-lg-4 col-form-label">Proje Durumu Açıklaması</label>
                                <div class="col-12 col-lg-8">
                                    <textarea asp-for="ProjectStatusDescription" class="form-control"></textarea>
                                    <span asp-validation-for="ProjectStatusDescription" class="text-danger"></span>
                                </div>
                            </div>

                            <!-- Project Status Description Date -->
                            <div class="form-group row">
                                <label asp-for="ProjectStatusDescriptionDate" class="col-12 col-lg-4 col-form-label">Proje Durumu Açıklaması Tarihi</label>
                                <div class="col-12 col-lg-8">
                                    <input asp-for="ProjectStatusDescriptionDate" type="text" class="form-control datepicker">
                                    <i class="fas fa-calendar-alt"></i>
                                    <span asp-validation-for="ProjectStatusDescriptionDate" class="text-danger"></span>
                                </div>
                            </div>

                        </div>

                        <!-- Default Values for the Database Date Part -->
                        <input type="hidden" asp-for="CreationDate" />
                        <input type="hidden" asp-for="UpdateDate" />

                        <!-- User ID for the Update -->
                        <input type="hidden" asp-for="UserID" />

                        <div class="col-12">
                            <div class="buttons mt-3 my-md-0">
                                <button type="submit" class="btn bg-success text-white"><i class="fas fa-save mr-2"></i> Kaydet</button>
                                <a asp-action="Index" class="btn btn-danger"><i class="fas fa-trash-alt mr-2"></i> Vazgeç</a>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

<script>
    $('#selectRequestingDepartment').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/Department/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectResponsibleDepartment, #selectProductionResponsibleDepartment').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/Department/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectProjectOwner, #selectProjectManager').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/Person/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectRequestingAuthority').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/Authority/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectServiceArea, #selectAdditionalServiceArea').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/ServiceArea/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectProjectImportance').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/ProjectImportance/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectProjectType').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/ProjectType/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectProjectStatus, #selectProjectProductionStatus').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/ProjectStatus/JsonSelectData',
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    $('#selectExternalOrganization').select2({
        theme: "ibb",
        language: "tr",
        ajax: {
            url: '/Organization/JsonSelectData',
            data: function (params) {

                let query = {
                    getExternals: true,
                    term: params.term
                }
                return query;
            },
            dataType: 'json',
            processResults: function (response) {

                return {
                    results: response.results,
                };
            }
        }
    });

    //Dependent Fields Management
    $("[data-dependent-on]").each(function () {
        //Hide all dependent fields by default until user makes a choice.
        $(this).hide();

        //Get the object, object's target and desired value.
        let field = $(this);
        let target = $(this).data("dependent-on");
        let desiredValue = $(this).data("dependent-value");
        let status = $(target).is(':checked') ? 'checked' : 'unchecked';

        //Let's check initial state first for UX.
        if (status == desiredValue) field.show();

        //Now every time user changes the input value of the target, certain fields will open or close on DOM.
        $(target).on("change", function () {
            status = $(this).is(':checked') ? 'checked' : 'unchecked';
            if (status == desiredValue) field.show();
            else field.hide();
        });

    });
</script>
}
